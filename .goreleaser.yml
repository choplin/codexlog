version: 2

before:
  hooks:
    - go mod tidy
    - go test ./...

builds:
  - id: agentlog
    main: ./cmd/agentlog
    binary: agentlog
    env:
      - CGO_ENABLED=0
    goos:
      - linux
      - darwin
    goarch:
      - amd64
      - arm64
    ldflags:
      - -s -w
      - -X main.version={{.Version}}

archives:
  - id: agentlog
    format: tar.gz
    name_template: >-
      {{ .ProjectName }}_
      {{- .Version }}_
      {{- title .Os }}_
      {{- if eq .Arch "amd64" }}x86_64
      {{- else if eq .Arch "386" }}i386
      {{- else }}{{ .Arch }}{{ end }}
      {{- if .Arm }}v{{ .Arm }}{{ end }}
    format_overrides:
      - goos: windows
        format: zip
    files:
      - LICENSE
      - README.md
      - CHANGELOG.md

checksum:
  name_template: 'checksums.txt'

changelog:
  sort: asc
  use: github
  filters:
    exclude:
      - '^docs:'
      - '^test:'
      - '^chore:'
      - Merge pull request
      - Merge branch
  groups:
    - title: Features
      regexp: '^feat.*'
      order: 0
    - title: Bug Fixes
      regexp: '^fix.*'
      order: 1
    - title: Others
      order: 999

release:
  footer: |
    ## Installation

    ### Homebrew
    ```bash
    brew install choplin/tap/agentlog
    ```

    ### Go Install
    ```bash
    go install github.com/choplin/agentlog/cmd/agentlog@{{ .Tag }}
    ```

    ### Binary Download
    Download the appropriate binary for your platform from the assets below.

brews:
  - repository:
      owner: choplin
      name: homebrew-tap
      token: "{{ .Env.HOMEBREW_TAP_GITHUB_TOKEN }}"
    homepage: https://github.com/choplin/agentlog
    description: "Browse, search, and analyze AI agent conversation logs"
    license: MIT
    dependencies:
      - name: go
        type: build
    test: |
      system "#{bin}/agentlog", "--version"
    install: |
      bin.install "agentlog"
